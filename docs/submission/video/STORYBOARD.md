# Final Submission Film Storyboard

Target runtime: 2:20 to 2:55

| Scene | Story beat | Primary visual |
|---:|---|---|
| 01 | Product people are blocked before prompting | Editorial problem/solution deck |
| 02 | PPP is a product REPL, not source hot reload | Conversation to nREPL/browser runtime diagram |
| 03 | First product outcome | Real Codex Snake prompt and running timer/keyboard game |
| 04 | Product iteration crosses the server boundary | Real account creation, UX repair, visible error, authenticated reload |
| 05 | The game becomes a service | Real authenticated server action and SQLite ranking persistence |
| 06 | A product decision reshapes the service | Real Game library and timer-driven Tetris with prior state preserved |
| 07 | Codex operated across the whole project | Product/code, runtime, verification/deployment, and this film |
| 08 | Close | Hosted URL and tagline |

Editing rules:

- Do not show PPP access, credentials, a terminal, source, diffs, local paths,
  session identifiers, provider traces, or private diagnostics.
- Keep each real request screen visible, accelerate only its remaining wait
  footage at 4x with a small disclosure, and continue directly into the real
  outcome. Never imply real Codex generation was instantaneous.
- Do not replace a difficult live result with fake-provider footage.
- Say `nREPL` only in the explanatory deck and narration. The generated product
  UI remains nontechnical.
- State that the server workspace attaches to a running JVM through nREPL and
  that browser code evolves in a separate persistent sandbox runtime.
- State that Codex and GPT-5.6 supported product work, implementation,
  verification, deployment operations, and creation of the film itself.
