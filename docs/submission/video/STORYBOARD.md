# Final Submission Film Storyboard

Target runtime: 2:25 to 2:55

| Scene | Story beat | Primary visual |
|---:|---|---|
| 01 | Product people are blocked before prompting | Editorial problem/solution deck |
| 02 | PPP is a product REPL, not a file-save hot-reload demo | Conversation → bounded Shared POC → per-workspace nREPL destination |
| 03 | A product starts with taste and browser behavior | Real dark-theme request, rapid typed Snake request, timer and keyboard outcome |
| 04 | The product crosses the server boundary | Real product signup/sign-in request, accelerated continuous wait, account UI |
| 05 | Real failures become product feedback | Invalid identifier message, signup, logout, login, authenticated reload |
| 06 | One decision reshapes the service | Real library/Tetris request and same-session account/Snake/Tetris showcase |
| 07 | Codex operated across the whole project | Planning, implementation, presentation, film production, and deployed product agent |
| 08 | Close | Hosted URL and tagline |

Editing rules:

- Do not show PPP access, credentials, a terminal, source, diffs, local paths,
  session identifiers, provider traces, or private diagnostics.
- Show rapid human-like typing and the Enter key outcome. Do not paste a prompt
  instantaneously and do not leave the conversation scrolled above the result.
- Accelerate only the unbroken real provider wait at 24x or 30x with a visible
  badge, then continue directly into the actual result.
- Do not use a generation-compression interstitial or fake-provider footage.
- Explain the public Shared POC and per-workspace nREPL destination separately.
- The account demonstration must make a real HTTP request, show one real
  validation failure, and prove signup, logout, login, and reload persistence.
- State explicitly that Codex and GPT-5.6 contributed to product planning,
  implementation, presentation materials, film production, and the live agent
  running on the public test server.
