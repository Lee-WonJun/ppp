PPP is not a code generator followed by hot reload. It is an AI-era product REPL: evaluate behavior in long-lived server and browser runtimes, observe the real result, repair it, and checkpoint what works. This public judge build uses a bounded staged runtime. The self-hosted Workspace Capsule uses nREPL to redefine the running Clojure server directly.
