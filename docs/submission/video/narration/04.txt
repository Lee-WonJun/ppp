The next request crosses the server boundary without replacing Snake. It adds real product signup and sign-in, plus a SQLite-backed public profile. This identity belongs to the generated workspace. Only the PPP control plane, owner credentials, host, and other workspaces stay outside.
