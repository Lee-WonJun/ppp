The next request crosses the server boundary without replacing Snake. A real server action receives the live score and returns a one hundred point boost. The browser calls it, shows the response, reloads, and the server-powered feature is still there.
