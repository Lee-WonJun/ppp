Then I change the business rule in conversation. The same running action stops adding one hundred and starts multiplying the score by three. The same button now proves the new server behavior, with no separate build or deploy step.
