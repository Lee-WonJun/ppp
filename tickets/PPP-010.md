---
id: PPP-010
title: Harden cross-system tests and run the 24-case live evaluation
status: done
priority: P0
depends_on: [PPP-002, PPP-003, PPP-004, PPP-005, PPP-006, PPP-007, PPP-008, PPP-009]
requirements: [PRD-F01, PRD-F02, PRD-F04, PRD-F05, PRD-F11, PRD-F13, PRD-F19, PRD-F20, PRD-F21, PRD-F25, PRD-F26, PRD-F27, PRD-F29, PRD-F30]
---

# Outcome

All subsystem guarantees are exercised through realistic boundaries. A semantic Playwright suite proves the user flow, property tests prove the durable invariants, and an explicit OAuth run records 24 live provider outcomes without making live calls part of CI.

# Scope

- Complete all eight named property tests at required run counts.
- Fake-provider coordinator integration across every failure stage.
- Generated domain-test execution and provider repair feedback at the staged SQLite boundary.
- CLJS evaluator and state tests.
- Semantic Playwright access, no-refresh change, persistence, session, restore, stale-tab, and Safe Mode flows.
- Controlled security integration for CSRF, SCI, SQL, SSRF, connectors, logs, and quota.
- Live evaluator harness, result schema, sanitized reports, and scenario scoring.
- Flake diagnosis and deterministic fixtures.

# Acceptance Criteria

- [x] All PBT-01 through PBT-08 run at least 1,000 sequences and retain failing seeds.
- [x] E2E asserts business outcomes and persistence, not brittle presentation details.
- [x] Browser suite verifies no page reload during runtime changes.
- [x] Every implemented critical failure in the rubric has an executable test and user-safe result.
- [x] `bb verify` makes zero live provider calls.
- [x] `bb eval-live` is opt-in, requires readiness, runs 8x3, and writes a sanitized report.
- [x] LIVE-03, LIVE-04, LIVE-05 each pass 3/3 before video work.
- [x] No skipped critical test or silent fallback remains.

# Domain/PBT Tests

This ticket owns cross-system composition, not deferred unit testing. Each earlier ticket must already include its domain tests. Add only properties that require the real coordinator/browser/package boundary and regression coverage discovered during hardening.

Do not test exact copy, CSS classes, DOM nesting, private calls, progress count, or pixel snapshots.

# Evidence

- full `bb verify`: 126 JVM tests / 707 assertions, 17 CLJS tests / 73 assertions,
  eight Playwright tests, Docker smoke, and a 690-file secret scan
- stable-port cold-session run: three of three with a 6.5-second frame delay
- real Codex Tetris live smoke with timer, keyboard, and changing playfield
- generated business-rule regression: staged tests fail closed, feed bounded
  test identifiers to the same Codex repair thread, and commit only the corrected attempt
- property run counts and seeds
- deterministic retained seeds: PBT-01 `7001`, PBT-02 `7002`, PBT-03
  `8003`, PBT-04 `8004`, PBT-05 `90051` and `90052`, PBT-06 `90061`
  through `90064`, PBT-07 `9007`, and PBT-08 `7008`
- Playwright HTML report and selected traces/screenshots
- `artifacts/live-eval/20260715-222618/report.edn`: 24/24 passed; every LIVE-01
  through LIVE-08 scenario passed 3/3, with no prompts, generated source, or secrets
- critical failure coverage matrix

# Out of Scope

- Performance load testing for public SaaS scale.
- Cross-browser support beyond packaged Chromium gate, with manual Firefox/Safari notes optional.
- Automatically spending OAuth quota in CI.
